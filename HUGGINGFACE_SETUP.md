# 🤗 Hugging Face 开源模型设置指南

## ✅ 已完成的更新

### 1. 添加了 HuggingFaceProvider 类
- **位置**: `ncsu_advanced_config_base.py`
- **功能**: 支持使用 Hugging Face Inference API 调用开源模型
- **特点**: 
  - ✅ 完全免费（无需 API 密钥）
  - ✅ 支持多种开源模型
  - ✅ 自动处理模型加载
  - ✅ 支持可选的 HF_TOKEN（提高速率限制）

### 2. 更新了用户界面
- **默认 Provider**: 现在默认为 `huggingface`（免费）
- **模型建议**: 显示推荐的免费模型列表
- **API Key 检查**: 根据选择的 provider 显示相应的状态

### 3. 更新了依赖
- 添加了 `huggingface_hub>=0.20.0` 到 `requirements.txt`

## 🚀 如何使用

### 在 Hugging Face Spaces 上（推荐）

1. **无需设置任何 Secret** - 免费模型可以直接使用！

2. **可选**: 如果想提高速率限制，可以添加：
   - **Name**: `HF_TOKEN`
   - **Value**: 你的 Hugging Face token（从 https://huggingface.co/settings/tokens 获取）

3. **在界面中选择**:
   - Provider: `huggingface`
   - Model: `mistralai/Mistral-7B-Instruct-v0.2`（推荐）

### 在本地使用

1. **安装依赖**:
   ```bash
   pip install -r requirements.txt
   ```

2. **运行应用**:
   ```bash
   streamlit run user_interface.py
   ```

3. **选择 Hugging Face Provider**:
   - 在侧边栏选择 `huggingface`
   - 输入模型ID（如 `mistralai/Mistral-7B-Instruct-v0.2`）

## 🎯 推荐的免费模型

### 1. Mistral-7B-Instruct (最推荐)
- **模型ID**: `mistralai/Mistral-7B-Instruct-v0.2`
- **特点**: 性能优秀，响应速度快
- **适用**: 通用问答、内容生成

### 2. Llama 3.1 8B
- **模型ID**: `meta-llama/Llama-3.1-8B-Instruct`
- **特点**: Meta 开源，性能强劲
- **适用**: 复杂推理、长文本生成

### 3. Gemma 7B
- **模型ID**: `google/gemma-7b-it`
- **特点**: Google 开源，指令跟随能力强
- **适用**: 指令执行、结构化输出

### 4. Phi-3 Mini
- **模型ID**: `microsoft/Phi-3-mini-4k-instruct`
- **特点**: 轻量级，速度快
- **适用**: 快速响应、简单任务

## 💡 优势

### vs OpenAI/Anthropic:
- ✅ **完全免费** - 无需 API 密钥
- ✅ **开源透明** - 模型代码公开
- ✅ **无使用限制** - 免费层有合理的速率限制
- ✅ **隐私友好** - 数据不会用于训练

### 注意事项:
- ⚠️ 首次使用模型需要加载时间（30秒-2分钟）
- ⚠️ 免费层有速率限制（每分钟约30次请求）
- ⚠️ 响应质量可能略低于 GPT-4，但通常足够好

## 🔧 技术细节

### API 调用方式
- 使用 Hugging Face Inference API
- 端点: `https://api-inference.huggingface.co/models/{model}`
- 支持所有公开的文本生成模型

### Token 处理
- 如果设置了 `HF_TOKEN`，会自动使用
- 如果没有 token，仍然可以使用免费模型
- Token 提供更高的速率限制

### 错误处理
- 自动处理模型加载（503 错误）
- 自动重试机制
- 友好的错误提示

## 📝 代码示例

```python
config = {
    'llm_provider': 'huggingface',
    'llm_model': 'mistralai/Mistral-7B-Instruct-v0.2',
    'llm_temperature': 0.7,
    'llm_max_tokens': 1000
}

researcher = NCSUAdvancedResearcher(config)
results = researcher.research("Your query here")
```

## 🎉 完成！

现在你可以：
1. ✅ 在 Hugging Face Spaces 上使用，**无需任何 API 密钥**
2. ✅ 使用完全免费的开源模型
3. ✅ 享受与 OpenAI/Anthropic 类似的功能

**Go Pack!** 🐺
